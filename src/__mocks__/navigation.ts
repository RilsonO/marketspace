const mockUseNavigation = () => {
  return {
    // Adicione aqui as funções do hook useNavigation que você utiliza nos testes
    // Por exemplo, navigate, reset, goBack, etc.
  };
};

export const useNavigation = mockUseNavigation;
